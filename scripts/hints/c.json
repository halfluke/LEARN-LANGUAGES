{
  "variables_01": {
    "hints": [
      "Declare `int age = 25;` in `main`.",
      "Print with `printf(\"%d\", age);` — omit `\\n` when the expected output has no newline."
    ]
  },
  "variables_02": {
    "hints": [
      "Use `const char *name = \"Rust\";` (matches the checked solution; string literal has static storage).",
      "`printf(\"%s 1.85\", name);` avoids `%f` locale quirks by printing the fractional part literally."
    ]
  },
  "variables_03": {
    "hints": [
      "Promote with `double y = (double)x;` (or assign the `int` to `double` implicitly).",
      "`printf(\"%.0f\", y);` prints `100` without a fractional part."
    ]
  },
  "variables_04": {
    "hints": [
      "`printf(\"%d %.0f  %s\", 0, 0.0, \"false\");` — note the two spaces before `false` for `0 0  false`."
    ]
  },
  "variables_05": {
    "hints": [
      "Before `main`, add `#define MAX_SCORE 100` and `#define MIN_SCORE 0`.",
      "`printf(\"%d %d\", MAX_SCORE, MIN_SCORE);`"
    ]
  },
  "variables_06": {
    "hints": [
      "Swap with a temporary: `int t = a; a = b; b = t;`.",
      "`printf(\"%d %d\", a, b);` should print `10 5`."
    ]
  },
  "variables_07": {
    "hints": [
      "`const char *host = \"localhost\";`, `int port = 8080;`, and `int debug = 1;`.",
      "`printf(\"%s %d %s\", host, port, debug ? \"true\" : \"false\");` prints the lowercase words `true`/`false`."
    ]
  },
  "ownership_01": {
    "hints": [
      "`const char *t = s;` copies the pointer; both refer to the same `\"hi\"` literal.",
      "`printf(\"%s\", t);` — no newline if the grader expects bare `hi`."
    ]
  },
  "ownership_02": {
    "hints": [
      "`int *t = s;` means `t` aliases the same storage as `s`.",
      "After `t[0] = 9;`, print `printf(\"%d%d\", s[0], s[1]);` for `92`."
    ]
  },
  "ownership_03": {
    "hints": [
      "`int b = a;` copies the value; only `b++` changes `b`.",
      "`printf(\"%d %d\", a, b);` → `3 4`."
    ]
  },
  "controlflow_01": {
    "hints": [
      "`if (age >= 18) puts(\"Adult\"); else puts(\"Minor\");` — `puts` adds a trailing newline."
    ]
  },
  "controlflow_02": {
    "hints": [
      "`for (int i = 1; i <= 5; i++) printf(\"%d\\n\", i);`"
    ]
  },
  "controlflow_03": {
    "hints": [
      "`switch (day) { ... }` with `case 2: puts(\"Tuesday\"); break;` — use `break` unless fall-through is intentional."
    ]
  },
  "controlflow_04": {
    "hints": [
      "`const char *colors[] = {...};` then `for (size_t i = 0; i < 3; i++) printf(\"%s\\n\", colors[i]);`"
    ]
  },
  "controlflow_05": {
    "hints": [
      "Chain `if / else if` from the highest band downward (`A` then `B` …).",
      "For `score == 85`, the branch is `puts(\"B\");`."
    ]
  },
  "controlflow_06": {
    "hints": [
      "Inside `for (int i = 1; i <= 10; i++)`, skip evens with `if (i % 2 == 0) continue;`.",
      "Print odds with `printf(\"%d\\n\", i);`."
    ]
  },
  "controlflow_07": {
    "hints": [
      "With `for (;;)`, print `i`, then increment; `if (i >= 5) break;` stops before printing `5`."
    ]
  },
  "functions_01": {
    "hints": [
      "Implement `static void greet(void)` using `printf(\"Hello\\n\");` or `puts(\"Hello\");`.",
      "Call `greet();` from `main`."
    ]
  },
  "functions_02": {
    "hints": [
      "In `greet(const char *name)`, use `printf(\"Hello, %s\\n\", name);`.",
      "Call `greet(\"Alice\");`."
    ]
  },
  "functions_03": {
    "hints": [
      "`static int add(int a, int b) { return a + b; }`",
      "`printf(\"%d\\n\", add(3, 4));`"
    ]
  },
  "functions_04": {
    "hints": [
      "The `int *ok` out-parameter is the error channel: `b == 0` must set `*ok = 0`.",
      "`int r = divide(10, 3, &ok); printf(\"%d \", r);` then call `divide(10, 0, &ok);` and `if (!ok) printf(\"<nil>\");`."
    ]
  },
  "functions_05": {
    "hints": [
      "`static int sum(const int *nums, int n)` should loop `i < n` and accumulate.",
      "`int nums[] = {1,2,3,4,5}; printf(\"%d\\n\", sum(nums, 5));`"
    ]
  },
  "functions_06": {
    "hints": [
      "Use a `static int` at file scope as the captured total for repeated `adder` calls.",
      "Each call adds `x` and returns the running sum (`5` then `15`)."
    ]
  },
  "functions_07": {
    "hints": [
      "`atexit(print_end);` registers `print_end` to run after `main` returns.",
      "Print `start`, register the handler, then `working` so `end` appears last."
    ]
  },
  "arrays_01": {
    "hints": [
      "Print `[`, then each `arr[i]` separated by spaces (`putchar(' ')` when `i > 0`), then `]`."
    ]
  },
  "arrays_02": {
    "hints": [
      "`for (size_t i = 0; i < 3; i++) printf(\"%d\\n\", nums[i]);`"
    ]
  },
  "arrays_03": {
    "hints": [
      "Length: `sizeof arr / sizeof arr[0]` (only for actual array objects, not pointer parameters).",
      "`printf(\"%zu\\n\", ...)` with a `size_t` count."
    ]
  },
  "arrays_04": {
    "hints": [
      "`int arr[5] = {10, 20};` zero-fills the rest; print with the same bracket style as `arrays_01`."
    ]
  },
  "arrays_05": {
    "hints": [
      "Use nested `for` loops over rows and columns: `printf(\"%d\\n\", matrix[r][c]);`."
    ]
  },
  "arrays_06": {
    "hints": [
      "`printf(\"%d %d\\n\", arr[0], arr[2]);` for first and last elements."
    ]
  },
  "arrays_07": {
    "hints": [
      "`int sum = 0;` then accumulate in a bounded `for` over five elements.",
      "`printf(\"%d\\n\", sum);`"
    ]
  },
  "slices_01": {
    "hints": [
      "`int s[3] = {1,2,3};` — print `[` then elements with spaces, then `]`, mirroring slice formatting."
    ]
  },
  "slices_02": {
    "hints": [
      "Starter already has five elements; reuse the bracket-print loop with `n == 5`."
    ]
  },
  "slices_03": {
    "hints": [
      "Logical length is `3`, backing capacity is `10`: `printf(\"%d %d\\n\", len, 10);`."
    ]
  },
  "slices_04": {
    "hints": [
      "`sizeof fruits / sizeof fruits[0]` counts string pointers.",
      "`printf(\"%zu\\n\", ...);`"
    ]
  },
  "slices_05": {
    "hints": [
      "Indices `1` through `3` inclusive: `for (int i = 1; i < 4; i++)`.",
      "Format as `[2 3 4]` with spaces between numbers."
    ]
  },
  "slices_06": {
    "hints": [
      "`#include <string.h>` — `memcpy(copy, original, sizeof original);` then assign the fourth slot.",
      "Print the 3-element original line, then the 4-element copy line, each in `[ ... ]` form."
    ]
  },
  "slices_07": {
    "hints": [
      "The merged `int a[] = {1,2,3,4,5};` is already concatenated; print it like other slice exercises."
    ]
  },
  "maps_01": {
    "hints": [
      "`const char *keys[] = {\"Alice\", \"Bob\"}; int vals[] = {25, 30};` — keep index alignment.",
      "`print_str_int_map(keys, vals, 2); putchar('\\n');` as in the starter."
    ]
  },
  "maps_02": {
    "hints": [
      "Define parallel arrays for `Math`/`Physics` and call `map_get_str_int(..., \"Math\", &found)`.",
      "`printf(\"%d\\n\", ...)` with the returned score."
    ]
  },
  "maps_03": {
    "hints": [
      "`const char *keys[] = {\"1\", \"2\"}; const char *vals[] = {\"One\", \"Two\"};`",
      "`print_str_str_map(keys, vals, 2);`"
    ]
  },
  "maps_04": {
    "hints": [
      "Model deletion by listing only remaining keys: `{\"a\", \"c\"}` with `{1, 3}`."
    ]
  },
  "maps_05": {
    "hints": [
      "`map_get_str_int(keys, vals, n, \"bananas\", &found);` leaves `found == 0`.",
      "`puts(found ? \"exists\" : \"not found\");`"
    ]
  },
  "maps_06": {
    "hints": [
      "Parallel `keys[]` / `vals[]` with three grades; loop `for (int i = 0; i < 3; i++)`."
    ]
  },
  "maps_07": {
    "hints": [
      "`const char *keys[] = {\"one\", \"two\", \"three\"};` — print `3` or `sizeof keys / sizeof keys[0]`."
    ]
  },
  "strings_01": {
    "hints": [
      "`const char *greeting = \"Hello, Go!\";` — `puts(greeting);` matches the reference solution."
    ]
  },
  "strings_02": {
    "hints": [
      "`char ch = 'A';`",
      "`printf(\"Character: %c\\nCode Point: %d\\n\", ch, (int)ch);`"
    ]
  },
  "strings_03": {
    "hints": [
      "`strstr(text, \"Hello\")` drives the Contains line (`\"true\"` / `\"false\"` strings).",
      "Copy `text` to a writable `char buf[]`, locate `\"World\"` with `strstr`, patch to `Go!` and terminate like the sample.",
      "`strtok` on `\"a,b,c\"` split by `\",\"` to print the first token `a`."
    ]
  },
  "strings_04": {
    "hints": [
      "`snprintf(line, sizeof line, \"%s is %d years old\", \"Alice\", 30);` then `puts(line);`",
      "`printf(\"Pi is %.2f\\n\", 3.14159);`"
    ]
  },
  "strings_05": {
    "hints": [
      "`strcpy` into `up`/`lo`, then loop bytes with `toupper` / `tolower` cast through `(unsigned char)`."
    ]
  },
  "strings_06": {
    "hints": [
      "Prefix: `strncmp(url, \"https://\", 8) == 0`.",
      "Suffix: compare `strcmp(url + strlen(url) - 4, \".com\")` when length ≥ 4."
    ]
  },
  "strings_07": {
    "hints": [
      "Mutable `char s[] = \"hello\";` — swap `s[i]` with `s[j]` for `i < j` moving inward."
    ]
  },
  "structs_01": {
    "hints": [
      "`struct Person { const char *name; int age; };`",
      "`struct Person p = {\"Alice\", 30}; printf(\"{%s %d}\\n\", p.name, p.age);`"
    ]
  },
  "structs_02": {
    "hints": [
      "`struct Point pt = {5, 10}; struct Point *p = &pt;`",
      "`p->y = 20;` then print `p->x` and `p->y`."
    ]
  },
  "structs_03": {
    "hints": [
      "`struct Book { char title[32]; char author[16]; int pages; };`",
      "Initialize as `{\"Go Programming\", \"\", 0}` to get `{Go Programming  0}` spacing in the reference."
    ]
  },
  "structs_04": {
    "hints": [
      "`struct Employee { struct Person person; const char *company; };`",
      "`struct Employee emp = {{ \"Bob\" }, \"Acme\" }; puts(emp.company);`"
    ]
  },
  "structs_05": {
    "hints": [
      "`struct Rectangle r = {0};` gives zero width/height.",
      "`printf(\"{%g %g}\\n\", r.w, r.h);`"
    ]
  },
  "structs_06": {
    "hints": [
      "`struct Counter c = {0}; c.value += 5;`"
    ]
  },
  "structs_07": {
    "hints": [
      "Compare strings with `strcmp(host, \"localhost\") == 0` and `int port == 8080`.",
      "Print `true` when both hold."
    ]
  },
  "interfaces_01": {
    "hints": [
      "`struct Rectangle { double w, h; };` plus `static double rect_area(struct Rectangle r) { return r.w * r.h; }` at file scope (standard C).",
      "`struct Rectangle rect = {3, 4}; printf(\"%g\\n\", rect_area(rect));`"
    ]
  },
  "interfaces_02": {
    "hints": [
      "`typedef int (*writer_fn)(const char *);`",
      "`writer_fn w = logger_write; printf(\"%d %s\", w(\"hello\"), \"hello\");` — matches the stubbed `return 5`."
    ]
  },
  "interfaces_03": {
    "hints": [
      "`double pi = 3.14159265358979323846; double circle = pi * 5.0 * 5.0; double rect = 12.0;`",
      "`printf(\"%.14g\\n\", circle > rect ? circle : rect);`"
    ]
  },
  "interfaces_04": {
    "hints": [
      "The exercise resolves to `const char *name = \"Alice\"; puts(name);` in the reference."
    ]
  },
  "interfaces_05": {
    "hints": [
      "`printf(\"int: %d\\nstring: hello\\n\", 42);`"
    ]
  },
  "interfaces_06": {
    "hints": [
      "`printf(\"int: %d\\nstring: hello\\nfloat64: %.2f\\n\", 42, 3.14);`"
    ]
  },
  "interfaces_07": {
    "hints": [
      "`puts(\"Woaf\"); puts(\"Mew\");`"
    ]
  },
  "methods_01": {
    "hints": [
      "`static void greet(const char *name) { printf(\"Hello, %s\\n\", name); }`",
      "`greet(\"Alice\");`"
    ]
  },
  "methods_02": {
    "hints": [
      "Keep `struct Person` visible to `set_name`.",
      "`snprintf(p->name, sizeof p->name, \"%s\", n);`"
    ]
  },
  "methods_03": {
    "hints": [
      "`struct Counter increment(struct Counter c) { c.value++; return c; }` — value semantics."
    ]
  },
  "methods_04": {
    "hints": [
      "`printf(\"Point(%d, %d)\\n\", 10, 20);`"
    ]
  },
  "methods_05": {
    "hints": [
      "`char result[32] = \"\"; strcat(result, \"Hello\"); strcat(result, \"World\"); puts(result);`"
    ]
  },
  "methods_06": {
    "hints": [
      "`printf(\"%s\\n\", 42 % 2 == 0 ? \"true\" : \"false\");`"
    ]
  },
  "methods_07": {
    "hints": [
      "`double pi = 3.14159265358979323846, r = 5.0;`",
      "`printf(\"%.14g\\n%.14g\\n\", pi * r * r, 2 * pi * r);`"
    ]
  },
  "pointers_01": {
    "hints": [
      "`printf(\"%d\\n\", *ptr);` after `int *ptr = &x;`"
    ]
  },
  "pointers_02": {
    "hints": [
      "`printf(\"%d\\n\", *ptr);`"
    ]
  },
  "pointers_03": {
    "hints": [
      "In `modify`: `*p *= 2;`",
      "`modify(&num);`"
    ]
  },
  "pointers_04": {
    "hints": [
      "`int *ptr = malloc(sizeof *ptr); *ptr = 99;`",
      "`printf(\"%d\\n\", *ptr); free(ptr);`"
    ]
  },
  "pointers_05": {
    "hints": [
      "`struct Person p = {\"Bob\"}; puts(p.name);` — or `struct Person *p = &person; puts(p->name);`"
    ]
  },
  "pointers_06": {
    "hints": [
      "`int tmp = *a; *a = *b; *b = tmp;` in `swap`."
    ]
  },
  "pointers_07": {
    "hints": [
      "`if (ptr == NULL) puts(\"nil\");` — with `ptr` initialized `NULL`, you always print `nil` here."
    ]
  },
  "errors_01": {
    "hints": [
      "After `divide(10, 0, &ok);`, `ok` is `0`; print the division-by-zero message."
    ]
  },
  "errors_02": {
    "hints": [
      "The adapted file just prints `User not found` for this stub."
    ]
  },
  "errors_03": {
    "hints": [
      "`snprintf(msg, sizeof msg, \"wrapped: %s\", \"base error\"); puts(msg);`"
    ]
  },
  "errors_04": {
    "hints": [
      "`puts(\"email\");`"
    ]
  },
  "errors_05": {
    "hints": [
      "`printf(\"%d\\n\", 10 / 2);` — no separate error object in this stub."
    ]
  },
  "errors_06": {
    "hints": [
      "`puts(\"not found\");`"
    ]
  },
  "errors_07": {
    "hints": [
      "First line: `snprintf(buf, sizeof buf, \"reading %s: %s\", \"data.txt\", \"disk full\");`",
      "Second: `puts(\"Disk is full\");`"
    ]
  },
  "concurrency_01": {
    "hints": [
      "`pthread_create(&t, NULL, worker, NULL);` where `worker` prints `Hello!`.",
      "Sleep ~100 ms via `nanosleep` on a `struct timespec`, then `pthread_join` and print `main done`."
    ]
  },
  "concurrency_02": {
    "hints": [
      "`pipe(fd);` — writer uses `fd[1]`, reader `fd[0]`.",
      "`write` the four bytes of `ping` without a newline if the expected output is bare `ping`."
    ]
  },
  "concurrency_03": {
    "hints": [
      "`snprintf(buf, sizeof buf, \"10 20\"); write(fd[1], buf, (size_t)n);`"
    ]
  },
  "concurrency_04": {
    "hints": [
      "Worker sleeps ~10 ms (`nanosleep`) before writing `from ch1` into the pipe."
    ]
  },
  "concurrency_05": {
    "hints": [
      "Producer writes three newline-terminated lines then `close`s the write end so `read` eventually returns 0.",
      "Reader loops `read` into a buffer and `fwrite`s to stdout until EOF."
    ]
  },
  "concurrency_06": {
    "hints": [
      "Create thread, `pthread_join` it, then repeat so IDs print `1`, `2`, `3` in order before `done`."
    ]
  },
  "concurrency_07": {
    "hints": [
      "`pthread_mutex_lock` around `words[count++] = ...`.",
      "After joins, `qsort` the two pointers with `strcmp` and print both lines."
    ]
  },
  "testing_01": {
    "hints": [
      "`static int sum(int a, int b) { return a + b; }`",
      "`printf(\"%d\", sum(2, 3));` — note: no newline in expected output."
    ]
  },
  "testing_02": {
    "hints": [
      "`multiply` returns `a * b`.",
      "`printf(\"%d %d %d\", multiply(2,3), multiply(0,5), multiply(-1,4));`"
    ]
  },
  "testing_03": {
    "hints": [
      "Return `-1` when `b == 0`.",
      "Print `ok` only if `divide(10,2) == 5 && divide(10,0) == -1`."
    ]
  },
  "testing_04": {
    "hints": [
      "`validate(-5)` should return text containing `negative`.",
      "`printf(strstr(validate(-5), \"negative\") ? \"yes\" : \"no\");`"
    ]
  },
  "testing_05": {
    "hints": [
      "`for (int i = 0; i < 10000; i++) (void)sum(2, 3);`",
      "`printf(\"done\");` with no newline."
    ]
  },
  "json_01": {
    "hints": [
      "Escape inner quotes in the format string: `{\"Name\":\"Alice\",\"Age\":30}` via `snprintf`."
    ]
  },
  "json_02": {
    "hints": [
      "`sscanf(json, \"{\\\"Name\\\":\\\"%15[^\\\"]\\\",\\\"Age\\\":%d}\", name, &age);`"
    ]
  },
  "json_03": {
    "hints": [
      "Emit snake_case keys with one `snprintf` into `buf`: `first_name`, `last_name`, `birth_year`."
    ]
  },
  "json_04": {
    "hints": [
      "Only `Server` is non-empty in this scenario: `{\"Server\":\"api.example.com\"}`."
    ]
  },
  "json_05": {
    "hints": [
      "`snprintf` builds the object; `sscanf(..., \"price\\\":%lf\", &price)` recovers the float (pattern must match your literal).",
      "Mirror the `Original:` / `Recovered:` lines from the reference."
    ]
  },
  "json_06": {
    "hints": [
      "Nest braces in the format string: `Address` object inside the top-level JSON object."
    ]
  },
  "json_07": {
    "hints": [
      "Build a two-element JSON array string with two `Name`/`Age` objects separated by a comma."
    ]
  },
  "json_08": {
    "hints": [
      "`sscanf` on the fixed array string with two `%[^\"]` name fields and two `%d` ages."
    ]
  },
  "json_09": {
    "hints": [
      "Parse `level` from the fixed JSON prefix; second line prints the exact nested JSON substring from the reference."
    ]
  },
  "time_01": {
    "hints": [
      "`struct tm tm = {0}; tm.tm_year = 2024 - 1900; tm.tm_mon = 0; tm.tm_mday = 1; tm.tm_hour = 12;`",
      "Use `timegm` (GNU/BSD) or your platform’s UTC helper, then `gmtime_r` + `strftime` for `%Y-%m-%d %H:%M:%S +0000 UTC`."
    ]
  },
  "time_02": {
    "hints": [
      "The reference hard-codes six `puts` lines (Tokyo calendar text); match them verbatim."
    ]
  },
  "time_03": {
    "hints": [
      "`puts(\"2h30m0s\"); puts(\"500ms\");` as in the adapted stub."
    ]
  },
  "time_04": {
    "hints": [
      "`sscanf` the calendar string into `struct tm` fields; normalize `tm_year`/`tm_mon`, convert with `timegm`, format with `strftime`."
    ]
  },
  "time_05": {
    "hints": [
      "`puts(\"07/04/2024\"); puts(\"Thursday, July 4, 2024\");`"
    ]
  },
  "time_06": {
    "hints": [
      "`time_t t = timegm(&tm_jan15_2024) + (3 * 24 + 5) * 3600;` then format with `+0000 UTC` suffix."
    ]
  },
  "time_07": {
    "hints": [
      "`puts(\"720h0m0s\"); puts(\"Days: 30\");`"
    ]
  },
  "time_08": {
    "hints": [
      "Fixed instant in `struct tm` (2024-06-01 10:15:30 UTC); `strftime` once, print hour/minute from `struct tm`, add one second to `time_t`, format again."
    ]
  },
  "time_09": {
    "hints": [
      "`puts` the two boolean sentences exactly as in the expected output."
    ]
  }
}
