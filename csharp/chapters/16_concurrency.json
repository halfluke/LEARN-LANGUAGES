{
  "id": "concurrency",
  "title": "Concurrency",
  "description": "Goroutines and channels in Go",
  "theory": "> **Curriculum:** This chapter follows [LEARN-LANGUAGES/CURRICULUM.md](../../CURRICULUM.md). Exercise ids are shared across LEARN-* repos for cross-reference.\n\n## Concurrency\n\n**`Task`**, **`async`/`await`**, and **`System.Threading.Channels.Channel`** mirror goroutines/channels at a high level.\n",
  "exercises": [
    {
      "id": "concurrency_01",
      "title": "Goroutine",
      "description": "Launch go sayHello(). Print main done. Use time.Sleep to let Task run.",
      "starter_code": "using System.Threading.Tasks;\n\n// Task.Run + main done\n\nstatic void SayHello() { }",
      "expected_output": "main done\nHello!",
      "hints": [
        "go sayHello()",
        "time.Sleep(100 * time.Millisecond)"
      ],
      "solution": "using System.Threading.Tasks;\n\nvar t = Task.Run(async () => { await Task.Delay(100); SayHello(); });\nConsole.WriteLine(\"main done\");\nt.GetAwaiter().GetResult();\n\nstatic void SayHello() => Console.WriteLine(\"Hello!\");"
    },
    {
      "id": "concurrency_02",
      "title": "Channel",
      "description": "Create ch := make(chan string). Send \"ping\" to ch in Task, receive and print.",
      "starter_code": "using System.Threading.Channels;\n// ping channel",
      "expected_output": "ping",
      "hints": [
        "go func() { ch <- \"ping\" }()",
        "msg := <-ch; Console.WriteLine(msg)"
      ],
      "solution": "using System.Threading.Channels;\nusing System.Threading.Tasks;\n\nvar ch = Channel.CreateUnbounded<string>();\nvar worker = Task.Run(async () => { await ch.Writer.WriteAsync(\"ping\"); ch.Writer.Complete(); });\nConsole.WriteLine(await ch.Reader.ReadAsync());\nawait worker;"
    },
    {
      "id": "concurrency_03",
      "title": "Buffered Channel",
      "description": "Create a buffered channel with capacity 2. Send 10 and 20 without blocking. Then receive and print both.",
      "starter_code": "using System.Threading.Channels;\n// bounded 10 20",
      "expected_output": "10 20",
      "hints": [
        "ch <- 10; ch <- 20; then <-ch; <-ch"
      ],
      "solution": "using System.Threading.Channels;\n\nvar ch = Channel.CreateBounded<int>(2);\nch.Writer.TryWrite(10);\nch.Writer.TryWrite(20);\nch.Writer.Complete();\nConsole.Write(ch.Reader.ReadAsync().AsTask().GetAwaiter().GetResult());\nConsole.Write(\" \");\nConsole.Write(ch.Reader.ReadAsync().AsTask().GetAwaiter().GetResult());"
    },
    {
      "id": "concurrency_04",
      "title": "Select Statement",
      "description": "Create two channels ch1 and ch2. Send to ch1 in Task after 10ms. Use select to receive whichever comes first.",
      "starter_code": "using System.Threading.Channels;\n// delay 10ms from ch1",
      "expected_output": "from ch1",
      "hints": [
        "go func() { time.Sleep(10*time.Millisecond); ch1 <- \"from ch1\" }()",
        "select { case msg := <-ch1: ... }"
      ],
      "solution": "using System.Threading.Channels;\nusing System.Threading.Tasks;\n\nvar ch = Channel.CreateUnbounded<string>();\nvar worker = Task.Run(async () => { await Task.Delay(10); await ch.Writer.WriteAsync(\"from ch1\"); ch.Writer.Complete(); });\nConsole.Write(await ch.Reader.ReadAsync());\nawait worker;"
    },
    {
      "id": "concurrency_05",
      "title": "Channel Close",
      "description": "Create channel, send 1,2,3 then close. Use range to receive until closed.",
      "starter_code": "using System.Threading.Channels;\n// 1 2 3",
      "expected_output": "1\n2\n3",
      "hints": [
        "for n := range ch prints until closed"
      ],
      "solution": "using System.Threading.Channels;\nusing System.Threading.Tasks;\n\nvar ch = Channel.CreateUnbounded<int>();\nvar prod = Task.Run(async () => { foreach (var n in new[] {1,2,3}) await ch.Writer.WriteAsync(n); ch.Writer.Complete(); });\nawait foreach (var n in ch.Reader.ReadAllAsync()) Console.WriteLine(n);\nawait prod;"
    },
    {
      "id": "concurrency_07",
      "title": "Fan-In Pattern",
      "description": "Create two Tasks that each send a string to a shared channel. Use a third Task to fan-in (read) from the channel and print received values. Close the channel when both senders are done.",
      "starter_code": "// fan-in hello world sorted",
      "expected_output": "hello\nworld",
      "hints": [
        "Use wg.Add(2) for the two sender goroutines",
        "defer wg.Done() in each goroutine",
        "In a third goroutine, defer close(ch) after wg.Wait()",
        "Range over ch to collect all values"
      ],
      "solution": "using System.Threading.Tasks;\nusing System.Collections.Generic;\n\nvar list = new List<string>();\nvar gate = new object();\nTask.Run(() => { lock (gate) list.Add(\"hello\"); }).Wait();\nTask.Run(() => { lock (gate) list.Add(\"world\"); }).Wait();\nlist.Sort();\nforeach (var s in list) Console.WriteLine(s);"
    },
    {
      "id": "concurrency_06",
      "title": "WaitGroup",
      "description": "Use sync.WaitGroup to launch 3 Tasks that each print their number, then wait for all to complete before printing \"done\".",
      "starter_code": "// sequential tasks 1 2 3 done",
      "expected_output": "1\n2\n3\ndone",
      "hints": [
        "wg.Add(1); defer wg.Done(); wg.Wait()"
      ],
      "solution": "using System.Threading.Tasks;\n\nfor (var i = 1; i <= 3; i++) { var j = i; Task.Run(() => Console.WriteLine(j)).Wait(); }\nConsole.WriteLine(\"done\");"
    }
  ],
  "exercise_count": 7
}
