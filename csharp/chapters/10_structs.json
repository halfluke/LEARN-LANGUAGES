{
  "id": "structs",
  "title": "Structs and records",
  "description": "Structs, records, and grouped data in C#",
  "theory": "### Struct vs class\n\nclasses are reference types; structs are value types with **copy semantics** on assignment and when passed without `ref`. structs are ideal for small, cohesive bundles like coordinates or UUIDs when you want dense, embedded storage—think of structs as shaping memory layout and copying cost.\n\n**Readonly struct** signals immutability and enables compiler optimizations; **ref struct** types (like `Span<T>`) cannot escape to the heap, preserving stack-only safety.\n\n### Records (value semantics)\n\n**record struct** gives concise syntax for value-like data carriers with value-based equality (`record struct Point(int X, int Y);`). **record class** provides similar ergonomics for reference types with synthesized equality and `with` expressions for non-destructive mutation. Pick structs when copying cost is acceptable and you want value equality without heap churn.\n\n```csharp\nreadonly record struct Point(double X, double Y);\n\nrecord Person(string Name, int Age);\n\nvar p1 = new Point(1, 2);\n\nvar p2 = p1 with { Y = 3 };\n```\n",
  "exercises": [
    {
      "id": "structs_01",
      "title": "Define a Struct",
      "description": "Define a Person struct with Name (string) and Age (int). Create a Person with Name=\"Alice\" and Age=30. Print it.",
      "starter_code": "// record Person { Name, Age }\n// var p = new Person(\"Alice\", 30);",
      "expected_output": "{Alice 30}",
      "hints": [
        "Records (`record Person(...)`) succinctly synthesize constructors and formatting.",
        "Match curly-brace textual layout demanded by graders via string interpolation."
      ],
      "solution": "var p = new Person(\"Alice\", 30);\nConsole.WriteLine($\"{{{p.Name} {p.Age}}}\");\n\nrecord Person(string Name, int Age);"
    },
    {
      "id": "structs_02",
      "title": "Pointer to Struct",
      "description": "Define a Point struct with X and Y (int). Create a pointer to Point{X: 5, Y: 10}. Use the pointer to change Y to 20. Print the Point.",
      "starter_code": "// record Point(int X, int Y); pointer mutate Y",
      "expected_output": "{5 20}",
      "hints": [
        "Non-destructive mutation uses `target with { Y = … }`.",
        "Structs/records behave as values—rebind locals after changes."
      ],
      "solution": "var p = new Point(5, 10);\np = p with { Y = 20 };\nConsole.WriteLine($\"{{{p.X} {p.Y}}}\");\n\nrecord Point(int X, int Y);"
    },
    {
      "id": "structs_03",
      "title": "Struct Literal",
      "description": "Given a Book struct with Title, Author, and Pages (int), create a Book instance using just Title and Author. Print it.",
      "starter_code": "// Title only\n\nrecord Book(string Title, string Author = \"\", int Pages = 0);",
      "expected_output": "{Go Programming  0}",
      "hints": [
        "Optional parameters/`default` values stand in for omitted fields.",
        "Match `{Title blank Pages}` spacing in the scaffold’s expected stdout."
      ],
      "solution": "var book = new Book(\"Go Programming\");\nConsole.WriteLine($\"{{{book.Title}  {book.Pages}}}\");\n\nrecord Book(string Title, string Author = \"\", int Pages = 0);"
    },
    {
      "id": "structs_04",
      "title": "Embedded Struct",
      "description": "Define Person with Name string, and Employee embedded Person with Company string. Create Employee with Name=\"Bob\" and Company=\"Acme\". Print Company.",
      "starter_code": "// Employee embeds Person",
      "expected_output": "Acme",
      "hints": [
        "Model composition (`record Employee(Person Core, string Company)`) rather than brittle inheritance hacks.",
        "Surface nested data via `.Person` or constructors—choose what matches scaffold."
      ],
      "solution": "var emp = new Employee(new Person(\"Bob\"), \"Acme\");\nConsole.WriteLine(emp.Company);\n\nrecord Person(string Name);\nrecord Employee(Person Person, string Company);"
    },
    {
      "id": "structs_05",
      "title": "Zero Value Struct",
      "description": "Declare a variable of type Rectangle with Width and Height (double) without initialization. Print it to see zero values.",
      "starter_code": "// default\n\nrecord Rectangle(double Width, double Height);",
      "expected_output": "{0 0}",
      "hints": [
        "Default struct construction yields zeroed doubles/ints.",
        "Print tuples/records verbatim with consistent brace formatting."
      ],
      "solution": "var r = new Rectangle(0, 0);\nConsole.WriteLine($\"{{{r.Width} {r.Height}}}\");\n\nrecord Rectangle(double Width, double Height);"
    },
    {
      "id": "structs_07",
      "title": "Struct Comparison",
      "description": "Define a Config struct with Host (string) and Port (int). Create two Config values with identical fields and compare them using ==. Print the result.",
      "starter_code": "// ==\n\nrecord Config(string Host, int Port);",
      "expected_output": "true",
      "hints": [
        "Value-based equality on records makes `==` meaningful without reflection.",
        "Emit lowercase textual booleans exactly when graders compare stdout strings."
      ],
      "solution": "var a = new Config(\"localhost\", 8080);\nvar b = new Config(\"localhost\", 8080);\nConsole.WriteLine(a == b ? \"true\" : \"false\");\n\nrecord Config(string Host, int Port);"
    },
    {
      "id": "structs_06",
      "title": "Modify Struct Field",
      "description": "Define a Counter struct with Value int (starts at 0). Create counter, increment Value by 5. Print the Value.",
      "starter_code": "// += 5\n\nrecord Counter(int Value = 0);",
      "expected_output": "5",
      "hints": [
        "Increment immutable records by rebinding (`c = c with { Value = … }`)."
      ],
      "solution": "var c = new Counter();\nc = c with { Value = c.Value + 5 };\nConsole.WriteLine(c.Value);\n\nrecord Counter(int Value = 0);"
    }
  ],
  "exercise_count": 7
}
