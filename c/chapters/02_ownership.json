{
  "id": "ownership",
  "title": "Values, copies, and references (Go)",
  "description": "Go has garbage collection and no Rust-style ownership, but values vs pointers still shape how data is shared.",
  "theory": "> **Curriculum:** This chapter follows [LEARN-LANGUAGES/CURRICULUM.md](../../CURRICULUM.md). Exercise ids are shared across LEARN-* repos for cross-reference.\n\n## Aliasing in C\n\nPointers can alias the same storage; integers copy by value. Be explicit about who owns `malloc`’d memory.\n",
  "exercises": [
    {
      "id": "ownership_01",
      "title": "String reassignment",
      "description": "Assign `s := \"hi\"`, assign `t := s`, print `t` with `fmt.Print` (no newline).",
      "starter_code": "#include <stdio.h>\n\nint main(void) {\n    const char *s = \"hi\";\n    // const char *t = s;\n    // printf(\"%s\", t);\n    return 0;\n}\n",
      "expected_output": "hi",
      "hints": [
        "Both pointers refer to the same string literal."
      ],
      "solution": "#include <stdio.h>\n\nint main(void) {\n    const char *s = \"hi\";\n    const char *t = s;\n    printf(\"%s\", t);\n    return 0;\n}\n"
    },
    {
      "id": "ownership_02",
      "title": "Slice header copy",
      "description": "Create `s := []int{1, 2}`, assign `t := s`, set `t[0] = 9`, print `s[0]` and `s[1]` concatenated as digits with `fmt.Print` (no space, no newline) → `92`.",
      "starter_code": "#include <stdio.h>\n\nint main(void) {\n    int s[] = {1, 2};\n    int *t = s;\n    // t[0] = 9;\n    // printf(\"%d%d\", s[0], s[1]);\n    return 0;\n}\n",
      "expected_output": "92",
      "hints": [
        "`t` aliases the array."
      ],
      "solution": "#include <stdio.h>\n\nint main(void) {\n    int s[] = {1, 2};\n    int *t = s;\n    t[0] = 9;\n    printf(\"%d%d\", s[0], s[1]);\n    return 0;\n}\n"
    },
    {
      "id": "ownership_03",
      "title": "Int copy",
      "description": "`a := 3`, `b := a`, increment `b` with `b++`, print `a` and `b` space-separated with `fmt.Print` (no trailing newline).",
      "starter_code": "#include <stdio.h>\n\nint main(void) {\n    int a = 3;\n    int b = a;\n    // b++;\n    // printf(\"%d %d\", a, b);\n    return 0;\n}\n",
      "expected_output": "3 4",
      "hints": [
        "Only `b` changes."
      ],
      "solution": "#include <stdio.h>\n\nint main(void) {\n    int a = 3;\n    int b = a;\n    b++;\n    printf(\"%d %d\", a, b);\n    return 0;\n}\n"
    }
  ],
  "exercise_count": 3
}
