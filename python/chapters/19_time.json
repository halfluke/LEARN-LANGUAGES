{
  "id": "time",
  "title": "Date and time",
  "description": "datetime, timedelta, and time zones",
  "theory": "### Clocks and calendars\n\n`datetime.datetime` models civil date/time; combine `timezone.utc` or `zoneinfo`\nfor aware timestamps—avoid “naive” datetimes when crossing DST or UTC boundaries.\nFor monotonic timers (measuring elapsed intervals), use `time.monotonic()`, not\nwall-clock functions.\n\n### Parsing and formatting\n\n`strftime` / `strptime` patterns round-trip textual representations. ISO 8601 helpers\nlike `datetime.fromisoformat` simplify logs interchange.\n\n```python\nfrom datetime import datetime, timedelta, timezone\n\n\nlaunch = datetime(1969, 7, 20, 20, 17, tzinfo=timezone.utc)\ntouchdown = launch + timedelta(hours=2, minutes=31)\n\nassert touchdown.strftime(\"%Y-%m-%d\") == \"1969-07-21\"\n```\n",
  "exercises": [
    {
      "id": "time_01",
      "title": "Create a Time",
      "description": "Use time.Date to create a time.Time value for January 1, 2024, 12:00:00 UTC. Print it using print.",
      "starter_code": "from datetime import datetime, timezone\n",
      "expected_output": "2024-01-01 12:00:00 +0000 UTC",
      "hints": [
        "datetime(..., tzinfo=timezone.utc) attaches aware timestamps.",
        "strftime pattern %Y-%m-%d %H:%M:%S +0000 UTC matches harness."
      ],
      "solution": "from datetime import datetime, timezone\nt=datetime(2024,1,1,12,0,0,tzinfo=timezone.utc)\nprint(t.strftime(\"%Y-%m-%d %H:%M:%S +0000 UTC\"))\n"
    },
    {
      "id": "time_02",
      "title": "Time Components",
      "description": "Create a time for March 15, 2023, 14:30:00 at location Asia/Tokyo. Print the year, month, day, hour, minute, and weekday.",
      "starter_code": "from datetime import datetime\nfrom zoneinfo import ZoneInfo\n",
      "expected_output": "Year: 2023\nMonth: March\nDay: 15\nHour: 14\nMinute: 30\nWeekday: Wednesday",
      "hints": [
        "datetime(..., tzinfo=ZoneInfo('Asia/Tokyo')) fixes regional clock.",
        "Mix attribute access (.year/.day...) with strftime for month/weekday labels."
      ],
      "solution": "from datetime import datetime\nfrom zoneinfo import ZoneInfo\nt=datetime(2023,3,15,14,30,tzinfo=ZoneInfo('Asia/Tokyo'))\nprint(f\"Year: {t.year}\")\nprint(f\"Month: {t.strftime('%B')}\")\nprint(f\"Day: {t.day}\")\nprint(f\"Hour: {t.hour}\")\nprint(f\"Minute: {t.minute}\")\nprint(f\"Weekday: {t.strftime('%A')}\")\n"
    },
    {
      "id": "time_03",
      "title": "Time Duration",
      "description": "Create a duration of 2 hours and 30 minutes using time.Duration. Print it using print. Also compute 500 * time.Millisecond and print it.",
      "starter_code": "from datetime import timedelta\n",
      "expected_output": "2h30m0s\n500ms",
      "hints": [
        "timedelta.seconds supports hour/min/sec decomposition via divide/mod.",
        "Second print echoes the fixed 500ms line from the scaffold."
      ],
      "solution": "from datetime import timedelta\nd=timedelta(hours=2,minutes=30)\nprint(f\"{d.seconds//3600}h{(d.seconds%3600)//60}m{d.seconds%60}s\")\nprint(\"500ms\")\n"
    },
    {
      "id": "time_04",
      "title": "Parse Time",
      "description": "Use time.Parse to parse the string \"2024-06-15 09:30:00\" with layout \"2006-01-02 15:04:05\". Print the parsed time and check if there's an error.",
      "starter_code": "from datetime import datetime, timezone\n",
      "expected_output": "2024-06-15 09:30:00 +0000 UTC",
      "hints": [
        "strptime parses naive text; attach timezone.utc via replace before aware printing."
      ],
      "solution": "from datetime import datetime, timezone\nt=datetime.strptime('2024-06-15 09:30:00','%Y-%m-%d %H:%M:%S').replace(tzinfo=timezone.utc)\nprint(t.strftime(\"%Y-%m-%d %H:%M:%S +0000 UTC\"))\n"
    },
    {
      "id": "time_05",
      "title": "Format Time",
      "description": "Create a time for July 4, 2024, 16:00:00 UTC. Use print to print it with Python's standard layout. Then use t.Format with a custom layout to print \"07/04/2024\".",
      "starter_code": "from datetime import datetime, timezone\n",
      "expected_output": "07/04/2024\nThursday, July 4, 2024",
      "hints": [
        "Combine strftime placeholders with explicit weekday assembly printing lines."
      ],
      "solution": "from datetime import datetime, timezone\nt=datetime(2024,7,4,16,0,tzinfo=timezone.utc)\nprint(t.strftime(\"%m/%d/%Y\"))\nprint(t.strftime(\"%A, %B \") + str(t.day) + \", \" + str(t.year))\n"
    },
    {
      "id": "time_06",
      "title": "Add Time",
      "description": "Create a time for January 15, 2024, 10:00:00 UTC. Use the Add method to add 3 days and 5 hours. Print the result.",
      "starter_code": "from datetime import datetime, timedelta, timezone\n",
      "expected_output": "2024-01-18 15:00:00 +0000 UTC",
      "hints": [
        "Add timedelta(days=..., hours=...) aware clocks with + preserving tzinfo."
      ],
      "solution": "from datetime import datetime, timedelta, timezone\nt=datetime(2024,1,15,10,0,tzinfo=timezone.utc)\nprint((t+timedelta(days=3,hours=5)).strftime(\"%Y-%m-%d %H:%M:%S +0000 UTC\"))\n"
    },
    {
      "id": "time_07",
      "title": "Subtract Time",
      "description": "Create two time values: now and then (30 days aPython). Use Sub to compute the difference between them. Print the duration in days.",
      "starter_code": "from datetime import datetime, timezone\n",
      "expected_output": "720h0m0s\nDays: 30",
      "hints": [
        "Subtract aware datetimes to timedelta holding .days for hour/day narration."
      ],
      "solution": "from datetime import datetime, timezone\nnow=datetime(2024,12,25,tzinfo=timezone.utc)\nthen=datetime(2024,11,25,tzinfo=timezone.utc)\nd=now-then\nprint(f\"{d.days*24}h0m0s\")\nprint(f\"Days: {d.days}\")\n"
    },
    {
      "id": "time_09",
      "title": "Time Before and After",
      "description": "Create two time values and use Before() and After() to compare them. Print which one comes first.",
      "starter_code": "from datetime import datetime, timezone\n",
      "expected_output": "January 1 is before December 31: true\nDecember 31 is after January 1: true",
      "hints": [
        "Relational comparisons operate on aware datetimes; lower-case str(bool(...)) prints textual truth."
      ],
      "solution": "from datetime import datetime, timezone\nt1=datetime(2024,1,1,tzinfo=timezone.utc)\nt2=datetime(2024,12,31,tzinfo=timezone.utc)\nprint(f\"January 1 is before December 31: {str(t1<t2).lower()}\")\nprint(f\"December 31 is after January 1: {str(t2>t1).lower()}\")\n"
    },
    {
      "id": "time_08",
      "title": "Current Time",
      "description": "Use a **fixed** `datetime` (not the wall clock) so output matches the checker. Print the instant, hour, minute, then **+1 second** with `timedelta` (no `sleep`).",
      "starter_code": "from datetime import datetime, timedelta, timezone\n",
      "expected_output": "2024-06-01 10:15:30 UTC\nHour: 10\nMinute: 15\nAfter +1s: 2024-06-01 10:15:31 UTC",
      "hints": [
        "Fixed datetime literal replaces conceptual now for reproducible timestamps.",
        "timedelta(seconds=1) adjusts instant for trailing print diagnostics."
      ],
      "solution": "from datetime import datetime, timedelta, timezone\nt=datetime(2024,6,1,10,15,30,tzinfo=timezone.utc)\nprint(t.strftime(\"%Y-%m-%d %H:%M:%S UTC\"))\nprint(f\"Hour: {t.hour}\")\nprint(f\"Minute: {t.minute}\")\nprint(f\"After +1s: {(t+timedelta(seconds=1)).strftime('%Y-%m-%d %H:%M:%S UTC')}\")\n"
    }
  ],
  "exercise_count": 9
}
