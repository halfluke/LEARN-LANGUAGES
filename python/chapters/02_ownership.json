{
  "id": "ownership",
  "title": "Values, copies, and references (Go)",
  "description": "Go has garbage collection and no Rust-style ownership, but values vs pointers still shape how data is shared.",
  "theory": "> **Curriculum:** This chapter follows [LEARN-LANGUAGES/CURRICULUM.md](../../CURRICULUM.md). Exercise ids are shared across LEARN-* repos for cross-reference.\n\n## References vs values\n\nLists and dicts are mutable references; integers and strings behave like copied values for assignment. This chapter maps Go ownership ideas to Python’s object model.\n",
  "exercises": [
    {
      "id": "ownership_01",
      "title": "String reassignment",
      "description": "Assign `s := \"hi\"`, assign `t := s`, print `t` with `print` (no newline).",
      "starter_code": "s = \"hi\"\n",
      "expected_output": "hi",
      "hints": [
        "`t := s` copies the string header, both refer to same bytes",
        "`fmt.Print(t)`"
      ],
      "solution": "s = \"hi\"\nt = s\nprint(t, end=\"\")\n"
    },
    {
      "id": "ownership_02",
      "title": "Slice header copy",
      "description": "Create `s := []int{1, 2}`, assign `t := s`, set `t[0] = 9`, print `s[0]` and `s[1]` concatenated as digits with `print` (no space, no newline) → `92`.",
      "starter_code": "s = [1, 2]\n",
      "expected_output": "92",
      "hints": [
        "`t := s` shares backing array",
        "`t[0] = 9` affects `s[0]`"
      ],
      "solution": "s = [1, 2]\nt = s\nt[0] = 9\nprint(f'{s[0]}{s[1]}', end='')\n"
    },
    {
      "id": "ownership_03",
      "title": "Int copy",
      "description": "`a := 3`, `b := a`, increment `b` with `b++`, print `a` and `b` space-separated with `print` (no trailing newline).",
      "starter_code": "a = 3\n",
      "expected_output": "3 4",
      "hints": [
        "`b := a` copies the `int`",
        "`b++`"
      ],
      "solution": "a = 3\nb = a\nb += 1\nprint(f'{a} {b}')\n"
    }
  ],
  "exercise_count": 3
}
